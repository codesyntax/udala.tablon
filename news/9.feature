Fixed an issue in the `GET /@tablon-expired` endpoint where returning the same multilingual Shared UID payload would duplicate results if queried across multiple languages natively. The endpoint now correctly resolves `Language="all"` directly through the catalog and cleanly deduplicates output objects using a unique `seen_shared_uids` set, ensuring symmetrically grouped outputs match the REST API POST signature exactly once.
